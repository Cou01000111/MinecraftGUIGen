resource pack{widgets base path,widgets chars path,widgets charsJson path,outputPath } + options.txt + options{widgets.pngの上書きか}
```json
resourcePack:{
    widgetsBasePath:string,
    widgetsCharsPath:string,
    widgetsJsonPath:string,
    outputPath:string
}

options.txt:{
    path:string,
    
}

options
```
